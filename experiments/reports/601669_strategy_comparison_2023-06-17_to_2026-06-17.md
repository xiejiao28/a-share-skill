# 601669 策略对比结论

- 样本区间：`2023-06-17` 到 `2026-06-17`
- 标的：`601669`

## 结论

当前这轮单票实验里，`rsi_revert` 明显优于 `sma_cross`。

| 策略 | 最优参数 | 收益率 | 最大回撤 | 收益回撤比 | 交易次数 |
| --- | --- | ---: | ---: | ---: | ---: |
| `sma_cross` | `fast=30`, `slow=60` | `16.41%` | `28.45%` | `0.5768` | `6` |
| `rsi_revert` | `buy_rsi=25`, `sell_rsi=60` | `17.47%` | `12.93%` | `1.3511` | `21` |

## 关键信息

- `sma_cross` 在扩展快线后，最优参数从 `15/60` 变成 `30/60`。
- `fast` 继续加大到 `40/50/60` 后，没有超过 `30/60`。
- `rsi_revert` 在把 `sell_rsi` 下探到 `50/55` 后，最优参数仍然是 `25/60`。
- 对当前样本来说，`sell_rsi=60` 比更早卖出的 `55` 或 `50` 更优。

## 当前可用结论

1. `rsi_revert 25/60` 是当前最优候选。
2. `sma_cross 30/60` 是当前均线策略最优候选。
3. 如果只在这只票上选一个策略，优先选 `rsi_revert`。

## 下一步

- 细化 `rsi_revert` 在 `buy_rsi=25` 附近的搜索密度。
- 再决定是否继续上探 `sma_cross` 的 `slow` 周期。
- 后续拿一个小股票池做批量验证，确认不是单票过拟合。
